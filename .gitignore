# sortie de build pour la publication Artifact (photos intégrées en base64)
src/artifact.html

# clés d'administration des compteurs : à ne jamais publier
CLES-ADMIN.txt

# dossier assemblé par src/publish.js à chaque mise en ligne
public/

# fichiers déposés par wrangler s'il est lancé à la main
.wrangler/

.dev.vars*
!.dev.vars.example
.env*
!.env.example
